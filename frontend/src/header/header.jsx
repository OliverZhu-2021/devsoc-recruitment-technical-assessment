import styled from "styled-components";

const HeaderWrapper = styled.div`
  width: 100%;
  height: 60px;
  background: green;
  position: fixed;
`;

const Header = () => {
  return(
    <HeaderWrapper>
    </HeaderWrapper>
  )
};

export default Header;